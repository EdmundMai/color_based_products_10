<%= form_for [:admin, @vendor] do |form| %>
	<ul>
		<% @vendor.errors.full_messages.each do |error| %>
			<li><%= error %></li>
		<% end %>
	</ul>

	<%= form.label :name %>
	<%= form.text_field :name %><br>
	
	<%= form.submit %>
<% end %>